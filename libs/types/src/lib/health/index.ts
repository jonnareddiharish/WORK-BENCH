// Health domain types — add new schemas here as features are built.
// Each schema must export both the Zod schema and its inferred TypeScript type.
//
// Pattern:
//   export const FooSchema = z.object({ ... });
//   export type Foo = z.infer<typeof FooSchema>;

export {};
